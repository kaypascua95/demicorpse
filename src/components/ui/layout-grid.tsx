import {useState,type ReactNode} from 'react';import {motion} from 'framer-motion';import {cn} from '@/lib/utils';
export type LayoutGridCard={id:string;content:ReactNode;className:string;thumbnail:string};
export function LayoutGrid({cards}:{cards:LayoutGridCard[]}){const[selected,setSelected]=useState<LayoutGridCard|null>(null);return <div className="gallery-layout-grid">{cards.map(card=><div key={card.id} className={cn('gallery-layout-cell',card.className)}><motion.button type="button" onClick={()=>setSelected(card)} className="gallery-layout-card" layoutId={'gallery-card-'+card.id}><motion.img layoutId={'gallery-image-'+card.id} src={card.thumbnail} alt="" loading="lazy"/></motion.button></div>)}{selected&&<div className="gallery-layout-modal" role="dialog" aria-modal="true" onClick={()=>setSelected(null)}><motion.div layoutId={'gallery-card-'+selected.id} className="gallery-layout-selected" onClick={e=>e.stopPropagation()}><motion.img layoutId={'gallery-image-'+selected.id} src={selected.thumbnail} alt=""/><div className="gallery-layout-overlay"/><motion.div className="gallery-layout-copy" initial={{opacity:0,y:40}} animate={{opacity:1,y:0}}>{selected.content}</motion.div><button className="gallery-layout-close" onClick={()=>setSelected(null)} aria-label="Close image">×</button></motion.div></div>}</div>}