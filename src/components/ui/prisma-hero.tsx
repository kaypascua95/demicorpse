import{useRef,useState}from"react";export function PrismaHero({onEntered}:{onEntered?:()=>void}){const[leaving,setLeaving]=useState(false);const video=useRef<HTMLVideoElement>(null);const enter=()=>{if(leaving)return;video.current?.play();setLeaving(true);setTimeout(()=>onEntered?.(),850)};return <section className="entrance-hero"><video ref={video} loop muted playsInline preload="auto" src="/demicorpse-hero-muted.mp4" className="hero-video instant-video"/><img src="/hero-still.png" alt="" className="hero-still instant-still"/><div className={"landing-lockup static-lockup "+(leaving?"text-leaving":"")}><div className="landing-rule"/><h1>DEMICORPSE</h1><p>SOMEWHERE BETWEEN ALIVE AND ARCHIVED.</p><button onClick={enter}><span>ENTER</span><i/></button></div></section>}