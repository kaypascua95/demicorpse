import{useRef,useState}from"react";export function PrismaHero({onEntered}:{onEntered?:()=>void}){const[entered,setEntered]=useState(false);const video=useRef<HTMLVideoElement>(null);const enter=()=>{video.current?.play();setEntered(true);requestAnimationFrame(()=>onEntered?.())};return <section className="entrance-hero"><video ref={video} loop muted playsInline preload="auto" src="/demicorpse-hero-muted.mp4" className="hero-video instant-video"/>{!entered&&<img src="/hero-still.png" alt="" className="hero-still instant-still"/>}{!entered&&<div className="landing-lockup static-lockup"><div className="landing-rule"/><h1>DEMICORPSE</h1><p>SOMEWHERE BETWEEN ALIVE AND ARCHIVED.</p><button onClick={enter}><span>ENTER</span><i/></button></div>}</section>}