"use client";import*as React from"react";import{motion,useMotionTemplate,useScroll,useTransform}from"framer-motion";interface Props{scrollHeight?:number;desktopImage:string;mobileImage:string;initialClipPercentage?:number;finalClipPercentage?:number;children?:React.ReactNode}export default function SmoothScrollHero({scrollHeight=1200,desktopImage,mobileImage,initialClipPercentage=18,finalClipPercentage=82,children}:Props){const{scrollY}=useScroll();const start=useTransform(scrollY,[0,scrollHeight],[initialClipPercentage,0]);const end=useTransform(scrollY,[0,scrollHeight],[finalClipPercentage,100]);const clipPath=useMotionTemplate`polygon(${start}% ${start}%, ${end}% ${start}%, ${end}% ${end}%, ${start}% ${end}%)`;const size=useTransform(scrollY,[0,scrollHeight+500],["145%","100%"]);return <div style={{height:`calc(${scrollHeight}px + 100vh)`}} className="relative w-full bg-[#0b0b0c]"><motion.div className="sticky top-0 h-screen w-full overflow-hidden bg-black" style={{clipPath,willChange:"clip-path"}}><motion.div className="absolute inset-0 md:hidden" style={{backgroundImage:`url(${mobileImage})`,backgroundSize:size,backgroundPosition:"center",backgroundRepeat:"no-repeat"}}/><motion.div className="absolute inset-0 hidden md:block" style={{backgroundImage:`url(${desktopImage})`,backgroundSize:size,backgroundPosition:"center",backgroundRepeat:"no-repeat"}}/><div className="absolute inset-0 bg-black/35"/><div className="noise absolute inset-0"/>{children}</motion.div></div>}