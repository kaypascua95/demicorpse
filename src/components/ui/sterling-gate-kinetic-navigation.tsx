import{useEffect,useState}from"react";import{motion,AnimatePresence}from"framer-motion";const links=[["FRAGMENTS","/fragments/"],["ARCHIVE","#archive"],["ABOUT","#about"],["ELSEWHERE","#elsewhere"],["COLOPHON","#colophon"]];export function KineticNavigation(){const[open,setOpen]=useState(false);useEffect(()=>{document.body.classList.toggle("menu-open",open);return()=>document.body.classList.remove("menu-open")},[open]);useEffect(()=>{const esc=(e:KeyboardEvent)=>e.key==="Escape"&&setOpen(false);window.addEventListener("keydown",esc);return()=>window.removeEventListener("keydown",esc)},[]);return <><button className="more-trigger" aria-expanded={open} onClick={()=>setOpen(true)}>MORE <span>+</span></button><AnimatePresence>{open&&<motion.aside className="more-drawer" initial={{x:"100%"}} animate={{x:0}} exit={{x:"100%"}} transition={{duration:.72,ease:[.16,1,.3,1]}}><div className="drawer-head"><span>MORE</span><button onClick={()=>setOpen(false)} aria-label="Close menu">×</button></div><nav>{links.map(([name,href])=><a className="kinetic-link" href={href} key={name} onClick={()=>setOpen(false)}>{name}</a>)}</nav><div className="drawer-rule"/><p>Somewhere between<br/>alive and archived.</p><div className="drawer-social">TWITCH · YOUTUBE · DISCORD · INSTAGRAM</div></motion.aside>}</AnimatePresence></>}