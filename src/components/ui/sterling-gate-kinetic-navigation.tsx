import{useEffect,useRef,useState}from"react";import{AnimatePresence,motion}from"framer-motion";const links=[["FRAGMENTS","/fragments/"],["ARCHIVE","#archive"],["ABOUT","#about"],["ELSEWHERE","#elsewhere"],["COLOPHON","#colophon"]];export function KineticNavigation(){const[open,setOpen]=useState(false);const root=useRef<HTMLDivElement>(null);useEffect(()=>{const outside=(e:PointerEvent)=>{if(root.current&&!root.current.contains(e.target as Node))setOpen(false)};const esc=(e:KeyboardEvent)=>e.key==="Escape"&&setOpen(false);window.addEventListener("pointerdown",outside);window.addEventListener("keydown",esc);return()=>{window.removeEventListener("pointerdown",outside);window.removeEventListener("keydown",esc)}},[]);return <div ref={root} className="more-nav"><button className="more-trigger" aria-expanded={open} onClick={()=>setOpen(v=>!v)}>MORE <span>{open?"−":"+"}</span></button><AnimatePresence>{open&&<motion.div className="more-dropdown" initial={{opacity:0,y:-10,scale:.98}} animate={{opacity:1,y:0,scale:1}} exit={{opacity:0,y:-8,scale:.985}} transition={{duration:.38,ease:[.16,1,.3,1]}}><nav>{links.map(([name,href],i)=><motion.a className="more-dropdown-link" href={href} key={name} onClick={()=>setOpen(false)} initial={{opacity:0,y:8}} animate={{opacity:1,y:0}} transition={{delay:.05+i*.045,duration:.35}}><span>0{i+1}</span>{name}</motion.a>)}</nav><div className="more-dropdown-foot">SOMEWHERE BETWEEN<br/>ALIVE AND ARCHIVED.</div></motion.div>}</AnimatePresence></div>}