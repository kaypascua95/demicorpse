import {entryTimestamp,type Entry} from '@/lib/cms';import {SiteLink} from '@/lib/navigation';
export default function Timeline06({entries,collection}:{entries:Entry[];collection:'journal'|'fragments'}){return <div className="dc-timeline"><div className="dc-timeline-inner">{entries.map(entry=><div className="dc-timeline-item" key={entry.id}><div className="dc-timeline-meta"><strong>{collection==='journal'?'JOURNAL':'FRAGMENT'}</strong><span>{entry.date}</span></div><div className="dc-timeline-content"><i/><SiteLink href={'/'+collection+'/'+entry.slug}><h3>{entry.title||entry.content.replace(/[#*_\x60]/g,'').slice(0,80)||'Untitled fragment'}</h3><p>{entry.excerpt||(collection==='fragments'?entry.content.replace(/[#*_\x60]/g,'').slice(0,180):'Open entry')}</p><small>{entryTimestamp(entry)}</small></SiteLink></div></div>)}</div></div>}